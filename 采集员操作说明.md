**开始前检查**：
1. 时钟同步，windows 时间点立即同步，手机连 wifi 后断开
2. 相机 app 检查腕部，后关闭
3. `conda activate record`
4. 开启 curry9 即可，不用任何操作
5. 连好 vive
6. Manus core 一定不开，手套亮蓝灯即可

**正式运行**：
1. 启动命令，开始完整一轮
    - 白班：`python scripts/run_session.py --session-dir data/session-day`
    - 夜班：`python scripts/run_session.py --session-dir data/session-night`
2. 开始会提示是否打乱，按 y 然后 enter
3. 每一条跑完，看结尾输出：“无错误”则没问题，按 enter 继续，如果有问题，按 r + enter 重跑，如果继续报错及时反馈

**结束**：
1. 数据统计，复制脚本输出的最终报告发到群里
2. 数据上传到云盘：
    - 白班：`data/session-day/2026-当月-当天-xx-xx-xx` 所有目录
    - 夜班：`data/session-night/2026-当月-当天-xx-xx-xx` 所有目录