**开始前检查**：
1. 时钟同步，windows 时间点立即同步，手机连 wifi 后断开
2. 相机 app 检查腕部，后关闭
3. 开启 curry9，new acquisition，点击蓝色三角形
4. 开启 steamvr，vivehub，连好 vive
5. Manus core 一定不开，手套亮蓝灯即可
6. 激活环境：`conda activate collect`
7. 切换工作目录：`cd C:/Projects/Embodied-Brain-Collect`
8. 硬件预检：`python scripts/preflight.py`，确保所有设备都能连上
9. 检查相机：`python scripts/check_cameras.py`，确保每个相机对应的窗口都能看到画面，并且对应的相机编号正确
10. 检查EMG：`python scripts/check_emg.py`，确保每个通道都能看到波形，分别摇晃左右手臂，对应的通道编号正确
11. 检查Vive：`python scripts/check_vive.py`，确保每个vive是对应的位置，分别摇晃左右手臂，对应的编号正确
12. 时钟同步：手机连上 wifi，等待 10s 再断开，电脑右键右下角时间 -> 调整日期和时间 -> 立即同步
13. 开启辅助员控制台（辅助员大屏全屏）：`python scripts/assist_console.py`
    左半屏 = stim 画面实时镜像，右半屏 = 第三相机实时画面；辅助员看这块屏判断采集阶段与采集员动作

**正式运行**：
1. 启动命令，开始完整一轮
    - 白班：`python scripts/run_session_env.py --session-dir data/session-day --collector-id L3-001`
    - 夜班：`python scripts/run_session_env.py --session-dir data/session-night --collector-id L3-002`
2. 每一条跑完，看结尾输出：“无错误”则没问题，按 n + enter 继续，如果有问题，按 r + enter 重跑，按 f + enter 失败退出（不记台账），如果继续报错及时反馈

**结束**：
1. 数据统计，复制脚本输出的最终报告发到群里：
    - 白班：`python scripts/session_summary.py --session-dir data/session-day`
    - 夜班：`python scripts/session_summary.py --session-dir data/session-night`
2. 数据上传到云盘：
    - 白班：`data/lerobot/session-day/2026-当月-当天/`
    - 夜班：`data/lerobot/session-night/2026-当月-当天/`
3. 数据上传到平台：
    - 打开网页：https://ei2rmd.baai.ac.cn/data-manage/sdk?projectId=251
    - 登录网页：账号 wushihan | 密码 Wushihan@2026
    - 复制ak/sk：点击右上角头像 -> API凭证 -> 复制即可
    - 启动上传程序：`.\start_upload.bat`
    - 粘贴该路径：`C:\Projects\Embodied-Brain-Collect\data\lerobot`，按enter，再空按一次enter
    - 登录网页：账号 wushihan | 密码 Wushihan@2026
    - 复制ak/sk：点击右上角头像 -> API凭证 -> 复制到程序中，按enter
    - 之后在网页上检查是否上传成功

**新图纸**：放到 `configs/environments/` 即可
